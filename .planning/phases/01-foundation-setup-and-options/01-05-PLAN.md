---
phase: 01-foundation-setup-and-options
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - vite.config.ts
  - src/main.tsx
  - src/styles.css
  - .github/workflows/deploy-pages.yml
autonomous: true
must_haves:
  truths:
    - "App builds with GitHub Pages-compatible asset base and hash-route runtime behavior."
    - "GitHub Actions can build static output and deploy the Pages artifact without manual branch pushes."
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite base-path configuration for project Pages hosting"
      contains: "base"
    - path: "src/main.tsx"
      provides: "SPA entrypoint bootstrapped with HashRouter"
      contains: "HashRouter"
    - path: ".github/workflows/deploy-pages.yml"
      provides: "Official GitHub Pages build/deploy workflow"
      contains: "actions/deploy-pages"
  key_links:
    - from: "package.json"
      to: "vite.config.ts"
      via: "bun run build executes Vite with Pages base config"
      pattern: "build"
    - from: ".github/workflows/deploy-pages.yml"
      to: "./dist"
      via: "upload-pages-artifact path"
      pattern: "path:\s*\./dist"
---

<objective>
Configure runtime/deployment wiring for GitHub Pages so the phase can validate real hash-based navigation and static deployment behavior.

Purpose: Isolate Pages-specific concerns from project bootstrap to keep plan scope focused and executable.
Output: Hash-router app entry, repo-base Vite config, foundational styles file, and official Pages Actions workflow.
</objective>

<execution_context>
@C:/Users/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup-and-options/01-CONTEXT.md
@.planning/phases/01-foundation-setup-and-options/01-RESEARCH.md
@.planning/phases/01-foundation-setup-and-options/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Pages-compatible runtime boot and base-path behavior</name>
  <files>vite.config.ts, src/main.tsx, src/styles.css</files>
  <action>Set Vite `base` for project-page deployment and ensure app boot uses `HashRouter` rather than browser history routing. Treat hash routing as locked for this phase to avoid GitHub Pages deep-link/refresh failures, and keep router mode explicit in source to prevent BrowserRouter regressions. Establish baseline global styles file used by subsequent UI plans.</action>
  <verify>Run `bun run build` and inspect `dist/index.html` output to confirm asset URLs include configured base prefix and app still compiles with hash-router entrypoint.</verify>
  <done>Runtime build output is Pages-compatible and app navigation is configured for static hosting deep-link safety.</done>
</task>

<task type="auto">
  <name>Task 2: Add official GitHub Pages Actions deployment workflow</name>
  <files>.github/workflows/deploy-pages.yml</files>
  <action>Create a Pages workflow using `actions/configure-pages`, `actions/upload-pages-artifact`, and `actions/deploy-pages` with required permissions (`contents: read`, `pages: write`, `id-token: write`). Use Bun for install/build and upload `./dist` artifact. Do not use legacy `gh-pages` branch push deployment.</action>
  <verify>Validate workflow YAML defines a build job and deploy job (`needs: build`) and that upload step targets `./dist`.</verify>
  <done>Repository includes an official Pages deployment pipeline aligned to GitHub's current Actions contract.</done>
</task>

</tasks>

<verification>
Confirm `bun run build` succeeds with hash-routing/base config and workflow YAML is ready for Pages deployment execution.
</verification>

<success_criteria>
DEP-01 through DEP-04 deployment/runtime wiring is complete: hash-router boot, Pages base-path support, and official Actions-based deployment workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup-and-options/01-05-SUMMARY.md`
</output>
